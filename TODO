add a arg in load_file which tells it to create the file if not found or no
add an option in update_txt_display that constrols how it tracks the current file column

#turns out the color pairs can be redefined