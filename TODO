make the text displaying function interpret the tabs as n spaces where n is the length of a tab(define this variable in simple_globals)
add a warp_text option to update_text_display

#turns out the color pairs can be redefined